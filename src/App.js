import React from 'react';
import LedgerInputWrapper from './component/LedgerInputWrapper';

export default function HelloWorld() {
    return (
        <div>
            <LedgerInputWrapper />
        </div>
    );
}